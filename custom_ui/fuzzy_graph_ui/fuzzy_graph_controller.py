from mining_algorithms.fuzzy_mining import FuzzyMining
from api.pickle_save import pickle_load

class FuzzyGraphController():
    def __int__(self, workingDirectory):# instead of dependency_threshold & min_frequency after workingDirectory I have to add my required variables for fuzzy mining algorithm
        super().__int__()
